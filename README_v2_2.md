# 🎯 new_detect_1_2.py - Версия с эквализацией только для поиска углов

## 💡 Главное отличие от new_detect_1.py:

**Эквализация гистограммы применяется ТОЛЬКО для улучшения поиска углов, все остальные этапы работают с оригинальными изображениями (без эквализации).**

## 🔄 Логика работы:

```
Исходные изображения
    ↓
ЭТАП 1: Устранение дисторсии
    ↓
ЭТАП 2: Поиск углов
    ├─ Создаются временные копии с эквализацией
    ├─ Углы находятся на эквализированных копиях
    └─ Найденные углы применяются к ОРИГИНАЛЬНЫМ изображениям
    ↓
ЭТАП 3: Растяжение плат (БЕЗ эквализации)
    ├─ Используются оригинальные изображения
    └─ Применяются найденные углы
    ↓
ЭТАП 4: Сравнение плат (БЕЗ эквализации)
    └─ Сравниваются оригинальные изображения
    ↓
ЭТАП 5: Визуализация
    └─ Показываются оригинальные изображения
```

## 🎯 Зачем это нужно?

### Проблема:
- Эквализация улучшает контраст для поиска углов
- Но может искажать цвета и яркость для сравнения

### Решение:
- ✅ Используем эквализацию для **улучшения поиска углов**
- ✅ Сохраняем **оригинальные цвета** для сравнения
- ✅ Получаем **точные углы** + **правильные цвета**

## 📊 Пример вывода:

```
======================================================================
СРАВНЕНИЕ ПЕЧАТНЫХ ПЛАТ
======================================================================

📂 ЗАГРУЗКА ИЗОБРАЖЕНИЙ
----------------------------------------------------------------------
Эталон: (1536, 2048, 3)
Тест:   (1418, 1895, 4)

🔧 ЭТАП 1: УСТРАНЕНИЕ ДИСТОРСИИ
----------------------------------------------------------------------
Эталонное изображение:
   ✓ Дисторсия устранена: (1536, 2048, 3) → (998, 1320, 3)
Тестовое изображение:
   ✓ Дисторсия устранена: (1418, 1895, 4) → (1034, 1409, 4)

Результат:
  Эталон: (998, 1320, 3)
  Тест:   (1034, 1409, 4)

🔍 ЭТАП 2: ПОИСК УГЛОВ ПЛАТ
----------------------------------------------------------------------
Применяем эквализацию для улучшения поиска углов...
   ✓ Эквализация гистограммы: CLAHE (clip=3.0, grid=(8, 8))
   ✓ Эквализация гистограммы: CLAHE (clip=3.0, grid=(8, 8))

Поиск углов на эквализированных изображениях:
   ✓ Найдено 392 точек контура, выбрано 4 крайних угла
   ✓ Найдено 287 точек контура, выбрано 4 крайних угла
Углы эталонного изображения: [(x1,y1), (x2,y2), (x3,y3), (x4,y4)]
Углы тестового изображения: [(x1,y1), (x2,y2), (x3,y3), (x4,y4)]
Матрица гомографии H вычислена успешно

[Важно] Углы найдены на эквализированных изображениях.
        Дальнейшая обработка использует ОРИГИНАЛЬНЫЕ изображения без эквализации.

📐 ЭТАП 3: РАСТЯЖЕНИЕ ПЛАТ ДО УГЛОВ ИЗОБРАЖЕНИЯ
----------------------------------------------------------------------
✓ Финальный размер обеих плат: (768, 1024, 3)
✓ Платы растянуты до углов изображения и готовы к сравнению

📊 ЭТАП 4: СРАВНЕНИЕ ПЛАТ
----------------------------------------------------------------------
=== СРАВНЕНИЕ ПЛАТ ===
Средняя разница: 0.0234
Макс. разница: 0.8923
Доля пикселей > 0.1: 12.34%

📈 ЭТАП 5: ВИЗУАЛИЗАЦИЯ РЕЗУЛЬТАТОВ
----------------------------------------------------------------------
Открытие графиков...

======================================================================
✅ СРАВНЕНИЕ ЗАВЕРШЕНО
======================================================================
```

## 🚀 Запуск:

```bash
python3 new_detect_1_2.py
```

## ⚙️ Настройка эквализации для поиска углов:

В строке ~1339:

```python
# Настройка CLAHE для поиска углов
reference_eq = equalize_histogram_lab(
    reference_array.copy(), 
    method='clahe',      # 'clahe', 'global' или 'none'
    clipLimit=3.0,       # Порог контраста (2.0-4.0)
    tileGridSize=(8, 8)  # Размер сетки
)
```

### Варианты:

**1. Стандартная CLAHE (по умолчанию):**
```python
method='clahe', clipLimit=3.0, tileGridSize=(8, 8)
```

**2. Более агрессивная (для сложных условий):**
```python
method='clahe', clipLimit=4.0, tileGridSize=(4, 4)
```

**3. Глобальная эквализация:**
```python
method='global'
```

**4. Без эквализации (как в new_detect_1.py без эквализации):**
```python
method='none'
```

## 📈 Графики:

### На графиках видно:
- **Верхний ряд [0,0] и [0,1]**: Оригинальные изображения (БЕЗ эквализации) с найденными углами
- **Нижний ряд**: Растянутые оригинальные изображения
- **Карта различий**: Основана на оригинальных цветах

**Подписи:**
- "дисторсия убрана, БЕЗ эквализации"
- "этап 3" для растянутых плат

## 🆚 Сравнение с new_detect_1.py:

| Параметр | new_detect_1.py | new_detect_1_2.py |
|----------|-----------------|-------------------|
| **Эквализация для поиска углов** | ❌ Нет | ✅ Да |
| **Эквализация для растяжения** | ❌ Нет | ❌ Нет |
| **Эквализация для сравнения** | ❌ Нет | ❌ Нет |
| **Точность поиска углов** | Хорошая | ✅ **Лучше** |
| **Сохранение цветов** | ✅ Да | ✅ Да |
| **Скорость** | Быстрее | Немного медленнее |

## 💡 Когда использовать new_detect_1_2.py:

### Используйте эту версию если:
- ✅ Сложные условия освещения
- ✅ Блики или тени на платах
- ✅ Низкий контраст краев платы
- ✅ Нужна **максимальная точность** поиска углов
- ✅ Важно сохранить **оригинальные цвета** для сравнения

### Используйте new_detect_1.py если:
- ✅ Хорошее освещение
- ✅ Четкие края платы
- ✅ Нужна максимальная скорость
- ✅ Простые условия съемки

## 🔧 Технические детали:

### Что происходит с памятью:
```python
# Создаются временные копии
reference_eq = equalize_histogram_lab(reference_array.copy(), ...)
test_eq = equalize_histogram_lab(test_array.copy(), ...)

# Углы находятся
ref_corners, test_corners = detector.find_corners(reference_eq, test_eq)

# Копии удаляются (автоматически)
# reference_eq и test_eq выходят из области видимости

# Дальше работаем с оригиналами
rectify_pcb_to_corners(reference_array, ref_corners)  # ← Оригинал!
```

### Почему .copy()?
```python
reference_array.copy()  # ← Создает копию, не изменяет оригинал
```

Без `.copy()` эквализация изменила бы оригинальный массив!

## 📝 Примечания:

1. **Углы корректны**: Эквализация не меняет геометрию, только контраст
2. **Координаты точны**: Углы, найденные на эквализированном изображении, точно соответствуют оригиналу
3. **Цвета оригинальны**: Сравнение происходит с реальными цветами плат
4. **Fallback работает**: Если эквализация не помогла, используется обычный алгоритм

## 🎉 Готово!

Эта версия дает **лучшую точность поиска углов** при **сохранении оригинальных цветов** для сравнения!

```bash
python3 new_detect_1_2.py
```

**Рекомендуется для production использования!** 🚀

