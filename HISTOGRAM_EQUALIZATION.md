# 🎨 Эквализация гистограммы в new_detect_1.py

## 📋 Что это?

Эквализация гистограммы - это техника улучшения контраста изображения путём перераспределения яркостей пикселей.

## 🎯 Зачем это нужно?

При сравнении печатных плат часто возникают проблемы:
- **Разное освещение** эталона и тестовой платы
- **Неравномерное освещение** на одном изображении
- **Низкий контраст** из-за условий съёмки
- **Блики и тени** на поверхности платы

**Эквализация решает эти проблемы!**

## 🔬 Как это работает?

### Обработка в LAB цветовом пространстве

1. **RGB → LAB конверсия**
   - L (Lightness) - яркость (0-100)
   - A - ось зелёный-красный
   - B - ось синий-жёлтый

2. **Эквализация только L-канала**
   - Сохраняет цветовую информацию
   - Улучшает только контраст

3. **LAB → RGB конверсия**
   - Возвращает изображение в исходное пространство

## 📊 Два метода эквализации

### 1. Глобальная эквализация (Global HE)

**Принцип:**
- Строит гистограмму яркостей всего изображения
- Вычисляет кумулятивную функцию распределения (CDF)
- Растягивает гистограмму на весь диапазон 0-255

**Плюсы:**
- ✅ Простая и быстрая
- ✅ Хорошо для равномерно освещённых изображений

**Минусы:**
- ❌ Может усилить шум
- ❌ Не учитывает локальные особенности
- ❌ Может "пережарить" яркие области

**Код:**
```python
equalize_histogram_lab(image, method='global')
```

### 2. Адаптивная эквализация CLAHE (Рекомендуется)

**CLAHE = Contrast Limited Adaptive Histogram Equalization**

**Принцип:**
- Разбивает изображение на сетку блоков (tiles)
- Эквализирует гистограмму в каждом блоке отдельно
- Ограничивает усиление контраста (clip limit)
- Интерполирует границы блоков

**Плюсы:**
- ✅ Адаптируется к локальным условиям
- ✅ Не усиливает шум чрезмерно
- ✅ Сохраняет детали в тёмных и светлых областях
- ✅ Идеально для неравномерного освещения

**Минусы:**
- ❌ Чуть медленнее глобальной

**Код:**
```python
equalize_histogram_lab(image, method='clahe', clipLimit=3.0, tileGridSize=(8, 8))
```

## 🎛️ Параметры CLAHE

### clipLimit (Порог ограничения контраста)

**Что это:**
- Максимальная высота гистограммы в блоке
- Предотвращает чрезмерное усиление шума

**Рекомендации:**
- `1.0` - минимальное усиление (консервативно)
- `2.0-4.0` - **стандартные значения** (рекомендуется)
- `5.0+` - агрессивное усиление (может усилить шум)

**По умолчанию:** `3.0`

**Пример:**
```python
# Консервативно (меньше артефактов)
equalize_histogram_lab(image, clipLimit=2.0)

# Стандартно (баланс)
equalize_histogram_lab(image, clipLimit=3.0)

# Агрессивно (максимальный контраст)
equalize_histogram_lab(image, clipLimit=5.0)
```

### tileGridSize (Размер сетки)

**Что это:**
- Размер блоков для адаптивной эквализации
- `(width, height)` в блоках

**Рекомендации:**
- `(4, 4)` - мелкие блоки, **локальная** адаптация
- `(8, 8)` - **стандартный** размер (рекомендуется)
- `(16, 16)` - крупные блоки, ближе к глобальной

**По умолчанию:** `(8, 8)`

**Пример:**
```python
# Очень локальная адаптация
equalize_histogram_lab(image, tileGridSize=(4, 4))

# Стандартная (баланс)
equalize_histogram_lab(image, tileGridSize=(8, 8))

# Более глобальная
equalize_histogram_lab(image, tileGridSize=(16, 16))
```

## 💡 Когда использовать что?

### Используйте CLAHE (по умолчанию):
- ✅ Неравномерное освещение
- ✅ Блики и тени на плате
- ✅ Разные условия съёмки эталона и теста
- ✅ Большинство реальных случаев

### Используйте глобальную эквализацию:
- ✅ Равномерное освещение
- ✅ Нужна максимальная скорость
- ✅ Простые условия съёмки

### Отключите эквализацию:
- ✅ Идеальные условия освещения
- ✅ Изображения уже обработаны
- ✅ Эквализация ухудшает результат

## 🔧 Как настроить в new_detect_1.py

### Место изменения:

Найдите в функции `main()` (примерно строка 1341):

```python
# ========== ЭТАП 2: ЭКВАЛИЗАЦИЯ ГИСТОГРАММЫ ==========
print("\n🎨 ЭТАП 2: ЭКВАЛИЗАЦИЯ ГИСТОГРАММЫ В LAB")
print("-"*70)
print("Эталонное изображение:")
reference_array = equalize_histogram_lab(reference_array, method='clahe', clipLimit=3.0, tileGridSize=(8, 8))
print("Тестовое изображение:")
test_array = equalize_histogram_lab(test_array, method='clahe', clipLimit=3.0, tileGridSize=(8, 8))
```

### Варианты настройки:

**1. Стандартная CLAHE (по умолчанию):**
```python
equalize_histogram_lab(image, method='clahe', clipLimit=3.0, tileGridSize=(8, 8))
```

**2. Консервативная CLAHE (меньше артефактов):**
```python
equalize_histogram_lab(image, method='clahe', clipLimit=2.0, tileGridSize=(8, 8))
```

**3. Агрессивная CLAHE (максимальный контраст):**
```python
equalize_histogram_lab(image, method='clahe', clipLimit=4.0, tileGridSize=(4, 4))
```

**4. Глобальная эквализация:**
```python
equalize_histogram_lab(image, method='global')
```

**5. Без эквализации:**
```python
equalize_histogram_lab(image, method='none')
# или просто закомментируйте строки:
# reference_array = equalize_histogram_lab(reference_array, ...)
```

## 📈 Визуализация результата

### Что показывают графики:

После эквализации можно построить графики гистограмм:

```python
import matplotlib.pyplot as plt

# L-канал до эквализации
L_before = rgb_to_lab_cv(image_uint8)[:, :, 0]
hist_before, _ = np.histogram(L_before.flatten(), 256, [0, 256])

# После эквализации
image_eq = equalize_histogram_lab(image, method='clahe')
L_after = rgb_to_lab_cv((image_eq * 255).astype(np.uint8))[:, :, 0]
hist_after, _ = np.histogram(L_after.flatten(), 256, [0, 256])

# График
plt.figure(figsize=(12, 4))
plt.subplot(1, 2, 1)
plt.bar(range(256), hist_before, color='gray', alpha=0.6)
plt.title('До эквализации')
plt.subplot(1, 2, 2)
plt.bar(range(256), hist_after, color='gray', alpha=0.6)
plt.title('После CLAHE')
plt.show()
```

## 🎯 Практические советы

### Для плат с бликами:
```python
# Более агрессивная локальная адаптация
equalize_histogram_lab(image, method='clahe', clipLimit=4.0, tileGridSize=(4, 4))
```

### Для плат с тенями:
```python
# Стандартная CLAHE хорошо справляется
equalize_histogram_lab(image, method='clahe', clipLimit=3.0, tileGridSize=(8, 8))
```

### Для разного освещения эталона и теста:
```python
# CLAHE выравнивает освещение обоих
equalize_histogram_lab(reference, method='clahe', clipLimit=3.0, tileGridSize=(8, 8))
equalize_histogram_lab(test, method='clahe', clipLimit=3.0, tileGridSize=(8, 8))
```

### Если эквализация ухудшает результат:
```python
# Попробуйте уменьшить clipLimit
equalize_histogram_lab(image, method='clahe', clipLimit=2.0, tileGridSize=(8, 8))

# Или отключите эквализацию
equalize_histogram_lab(image, method='none')
```

## 📊 Сравнение методов

| Параметр | Глобальная | CLAHE (4×4) | CLAHE (8×8) | CLAHE (16×16) |
|----------|-----------|-------------|-------------|---------------|
| **Скорость** | ⚡⚡⚡ | ⚡⚡ | ⚡⚡⚡ | ⚡⚡⚡ |
| **Локальная адаптация** | ❌ | ✅✅✅ | ✅✅ | ✅ |
| **Глобальный контраст** | ✅✅✅ | ✅ | ✅✅ | ✅✅✅ |
| **Артефакты** | Много | Мало | Очень мало | Минимум |
| **Для бликов** | ❌ | ✅✅✅ | ✅✅ | ✅ |
| **Для теней** | ❌ | ✅✅✅ | ✅✅ | ✅ |

## 🔬 Технические детали

### Поддержка форматов:

Функция работает с:
- ✅ RGB (3 канала)
- ✅ RGBA (4 канала) - автоматически конвертируется
- ✅ Grayscale (1 канал)
- ✅ float32 [0, 1]
- ✅ uint8 [0, 255]

### Зависимости:

- **OpenCV** (`cv2`) - обязательно для эквализации
- Если OpenCV недоступен, эквализация пропускается

### Проверка:

```bash
python3 -c "import cv2; print('✅ OpenCV доступен')"
```

## 🎉 Результат

С эквализацией гистограммы:
- ✅ **Улучшенный контраст** плат
- ✅ **Выравнивание освещения** между эталоном и тестом
- ✅ **Лучшее выделение дефектов** при сравнении
- ✅ **Робастность к условиям съёмки**

## 📝 Пример вывода

```
🎨 ЭТАП 2: ЭКВАЛИЗАЦИЯ ГИСТОГРАММЫ В LAB
----------------------------------------------------------------------
Эталонное изображение:
   ✓ Эквализация гистограммы: CLAHE (clip=3.0, grid=(8, 8))
Тестовое изображение:
   ✓ Эквализация гистограммы: CLAHE (clip=3.0, grid=(8, 8))

Результат после этапа 2:
  Эталон: (998, 1320, 3)
  Тест:   (1034, 1409, 3)
```

## 🚀 Быстрый старт

**По умолчанию всё уже настроено!** Просто запустите:

```bash
python3 new_detect_1.py
```

Эквализация CLAHE применится автоматически к обоим изображениям! 🎉

