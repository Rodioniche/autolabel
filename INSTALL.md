# 🔧 Установка зависимостей для new_detect_1.py

## ✅ Быстрая установка (рекомендуется)

Установите все зависимости одной командой:

```bash
pip install numpy pillow matplotlib scipy opencv-python scikit-image
```

## 📦 Пошаговая установка

### 1. Обязательные зависимости

Без них программа не запустится:

```bash
pip install numpy pillow matplotlib scipy
```

### 2. Устранение дисторсии (опционально)

Для функции `undistort_image()`:

```bash
pip install opencv-python
```

**Что будет без этого:**
```
🔧 ЭТАП 1: УСТРАНЕНИЕ ДИСТОРСИИ
   [Пропущено] OpenCV недоступен
```
Программа продолжит работу без устранения дисторсии.

### 3. Продвинутый поиск углов (НАСТОЯТЕЛЬНО рекомендуется)

Для функции `find_board_corners()`:

```bash
pip install scikit-image
```

**Что будет без этого:**
```
🔍 ЭТАП 2: ПОИСК УГЛОВ ПЛАТ
   [Пропущено] scikit-image недоступен, используется fallback алгоритм
   [Fallback] Используется простой алгоритм поиска углов
```
Программа автоматически переключится на fallback алгоритм из ноутбука.

## 🧪 Проверка установки

После установки запустите:

```bash
python3 -c "import numpy, PIL, matplotlib, scipy; print('✅ Основные пакеты установлены')"
python3 -c "import cv2; print('✅ OpenCV установлен')"
python3 -c "import skimage; print('✅ scikit-image установлен')"
```

## 🚀 Запуск программы

```bash
python3 new_detect_1.py
```

## 📋 Полный список пакетов

| Пакет | Назначение | Обязательность |
|-------|------------|----------------|
| `numpy` | Работа с массивами | ✅ Обязательно |
| `pillow` | Загрузка изображений | ✅ Обязательно |
| `matplotlib` | Визуализация | ✅ Обязательно |
| `scipy` | Интерполяция, обработка | ✅ Обязательно |
| `opencv-python` | Устранение дисторсии | 🟡 Опционально |
| `scikit-image` | Продвинутый поиск углов | 🟢 Рекомендуется |

## 🐍 Минимальная версия Python

Python 3.8 или новее

## 💡 Советы

1. **Для лучших результатов** устанавливайте все пакеты
2. **Если есть ошибки** при установке `opencv-python`, попробуйте `opencv-python-headless`
3. **Виртуальное окружение** рекомендуется для изоляции зависимостей:

```bash
python3 -m venv venv
source venv/bin/activate  # Linux/Mac
# или
venv\Scripts\activate  # Windows
pip install numpy pillow matplotlib scipy opencv-python scikit-image
```

## ❓ Проблемы

### scikit-image не устанавливается

Попробуйте обновить pip:

```bash
pip install --upgrade pip
pip install scikit-image
```

### OpenCV не работает

Используйте headless версию:

```bash
pip uninstall opencv-python
pip install opencv-python-headless
```

### Все равно не работает

Откройте issue с выводом команды:

```bash
pip list
python3 new_detect_1.py 2>&1
```

